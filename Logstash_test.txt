input {
  file {
    path => "/home/iiiedu/Desktop/data/jjj.csv"
    start_position => "beginning"
    sincedb_path => "/dev/null"
  }
}
filter {
  csv {
     separator => ","
     columns => ["dealTime","Usage","Type","Address","Pattern","Floor","floorSum","lat","long","unitPrice"]
  }
 
     mutate{convert => { "dealTime", "text" }}
     mutate{convert => { "Usage", "text" }}
     mutate{convert => { "Type", "text" }}
     mutate{convert => { "Address", "text" }}
     mutate{convert => { "Pattern", "text" }}
     mutate{convert => { "Floor", "long" }}
     mutate{convert => { "floorSum", "long" }}
     mutate{convert => { "lat", "float" }}
     mutate{convert => { "long", "float" }}
     mutate{convert => { "unitPrice", "long" }}
}
output {
  elasticsearch {
     hosts => "127.0.0.1:9200"
     index => "rent"
     document_type => "block"
  }
  stdout{}
}